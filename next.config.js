/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // allowedDevOrigins: ["http://localhost:5000"], ❌ Remova ou comente esta linha
  },
};

module.exports = nextConfig;
